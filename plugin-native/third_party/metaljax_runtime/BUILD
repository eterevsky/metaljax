# The build file below is not used by THIS package; it is handed to the
# @metaljax_runtime repository (see WORKSPACE), which is why it has to be an
# exported file rather than a plain BUILD.
exports_files(["BUILD.runtime"])
